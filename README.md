# homebrew-bambutop

Homebrew tap for [bambutop](https://github.com/rhoopr/bambutop) — a lightweight terminal monitoring tool for Bambu Lab 3D printers.

## Install

```bash
brew install rhoopr/bambutop/bambutop
```

Or tap first, then install:

```bash
brew tap rhoopr/bambutop
brew install bambutop
```

## Update

```bash
brew upgrade bambutop
```
